const blocks = JSON.parse(document.querySelector("#blocks-data").textContent);
const form = document.querySelector("#block-form"), sourceInput = document.querySelector("#source-input"), heading = document.querySelector("#form-heading"), recurringFields = document.querySelector("#recurring-fields"), oneTimeFields = document.querySelector("#one-time-fields"), cancelEdit = document.querySelector("#cancel-edit"), language = document.querySelector(".dashboard").dataset.language;
const dashboard = document.querySelector(".dashboard");
const titles = { timetable: language === "ko" ? "시간표" : "Timetable", manual: language === "ko" ? "직접 추가" : "Manual", google_calendar: "Google Calendar" };
function setScheduleType(type) { const recurring = type === "recurring"; recurringFields.hidden = !recurring; oneTimeFields.hidden = recurring; recurringFields.querySelectorAll("input, select").forEach((input) => { input.required = recurring; }); oneTimeFields.querySelectorAll("input").forEach((input) => { input.required = !recurring; }); }
function resetForm() { form.action = "/blocks"; form.reset(); document.querySelector('input[name="schedule_type"][value="recurring"]').checked = true; setScheduleType("recurring"); heading.textContent = `${titles[sourceInput.value]} ${language === "ko" ? "추가" : "block"}`; document.querySelector("#save-button").textContent = language === "ko" ? "추가" : "Add"; cancelEdit.hidden = true; }
document.querySelectorAll('input[name="schedule_type"]').forEach((radio) => radio.addEventListener("change", () => setScheduleType(radio.value)));
document.querySelectorAll(".source-tab").forEach((tab) => tab.addEventListener("click", () => { const source = tab.dataset.source; sourceInput.value = source; document.querySelectorAll(".source-tab").forEach((item) => item.classList.toggle("is-active", item === tab)); document.querySelectorAll("[data-source-list]").forEach((list) => { list.hidden = list.dataset.sourceList !== source; }); resetForm(); form.hidden = source === "google_calendar"; }));
document.querySelectorAll(".edit-block").forEach((button) => button.addEventListener("click", () => { const block = blocks.find((item) => item.id === Number(button.dataset.blockId)); sourceInput.value = block.source; form.hidden = false; form.action = `/blocks/${block.id}`; document.querySelector("#title-input").value = block.title; document.querySelector(`input[name="schedule_type"][value="${block.isRecurring ? "recurring" : "one_time"}"]`).checked = true; setScheduleType(block.isRecurring ? "recurring" : "one_time"); document.querySelector("#weekday-input").value = block.weekday ?? 0; document.querySelector("#start-time-input").value = block.startTime ?? ""; document.querySelector("#end-time-input").value = block.endTime ?? ""; document.querySelector("#starts-at-input").value = block.startsAt ?? ""; document.querySelector("#ends-at-input").value = block.endsAt ?? ""; heading.textContent = `${titles[block.source]} ${language === "ko" ? "수정" : "edit"}`; document.querySelector("#save-button").textContent = language === "ko" ? "저장" : "Save"; cancelEdit.hidden = false; }));
cancelEdit.addEventListener("click", resetForm);
const weekdays = language === "ko" ? ["월", "화", "수", "목", "금", "토", "일"] : ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"], grid = document.querySelector("#week-grid");
grid.innerHTML = `<div class="grid-corner"></div>${weekdays.map((day) => `<div class="day-heading">${day}</div>`).join("")}`;
for (let hour = 8; hour < 23; hour += 1) grid.insertAdjacentHTML("beforeend", `<div class="hour-label">${String(hour).padStart(2, "0")}:00</div>${weekdays.map(() => '<div class="hour-cell"></div>').join("")}`);
for (const block of blocks.filter((item) => item.isRecurring || (item.startsAt.slice(0, 10) >= dashboard.dataset.weekStart && item.startsAt.slice(0, 10) <= dashboard.dataset.weekEnd))) { const [startHour, startMinute] = block.startTime.split(":").map(Number), [endHour, endMinute] = block.endTime.split(":").map(Number), start = (startHour - 8) * 60 + startMinute, duration = (endHour * 60 + endMinute) - (startHour * 60 + startMinute); if (start < 0 || start > 900) continue; const item = document.createElement("div"); item.className = `preview-block source-${block.source}`; item.style.gridColumn = String(block.weekday + 2); item.style.gridRow = "2 / span 1"; item.style.transform = `translateY(${start}px)`; item.style.height = `${duration}px`; item.textContent = block.title; grid.append(item); }
